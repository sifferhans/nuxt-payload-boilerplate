{
  "name": "website",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "^0.1.0",
    "nuxt": "^3.2.0"
  },
  "dependencies": {
    "@vueuse/core": "^9.12.0",
    "@vueuse/nuxt": "^9.12.0"
  }
}